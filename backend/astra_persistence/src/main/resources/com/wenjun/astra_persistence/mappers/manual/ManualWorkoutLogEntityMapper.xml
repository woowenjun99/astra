<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wenjun.astra_persistence.mappers.manual.ManualWorkoutLogEntityMapper">
    <select id="getWorkoutLogs" resultType="com.wenjun.astra_persistence.models.WorkoutLogEntity">
        select
            calories_burnt as caloriesBurnt,
            date_created as dateCreated,
            duration,
            id,
            intensity,
            name
        from workout_log
        where user_id = ${userId}
        order by date_created desc
        limit ${pageNo} offset ${pageSize}
    </select>
</mapper>