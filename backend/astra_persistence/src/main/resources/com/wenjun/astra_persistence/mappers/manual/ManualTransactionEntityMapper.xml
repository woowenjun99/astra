<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wenjun.astra_persistence.mappers.manual.ManualTransactionEntityMapper">
    <select id="getSpendingByCategory" resultType="com.wenjun.astra_persistence.models.manual.CategoryWithSpending">
        select
            category,
            sum(amount) as amount
        from transactions
        where
            transaction_type = 0
            and user_id = #{userId}
            and date &gt; #{startDate}
            and date &lt; #{endDate}
        group by category
        order by amount desc
    </select>
</mapper>
